{
  "name": "ai-novel-to-screenplay",
  "version": "0.1.0",
  "private": true,
  "description": "AI-assisted tool that converts 3+ novel chapters into editable screenplay YAML.",
  "scripts": {
    "start": "python -m app.server",
    "check": "python scripts/smoke_check.py",
    "test": "python scripts/converter_tests.py"
  }
}
